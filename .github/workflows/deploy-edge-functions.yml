name: Deploy Supabase Edge Functions

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/functions/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy all edge functions
        run: |
          supabase functions deploy compute-impact --project-ref ${{ secrets.SUPABASE_PROJECT_REF }}
          supabase functions deploy send-email --project-ref ${{ secrets.SUPABASE_PROJECT_REF }}
          supabase functions deploy create-client-account --project-ref ${{ secrets.SUPABASE_PROJECT_REF }}
          supabase functions deploy admin-reset-password --project-ref ${{ secrets.SUPABASE_PROJECT_REF }}
          supabase functions deploy seed-users --project-ref ${{ secrets.SUPABASE_PROJECT_REF }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
